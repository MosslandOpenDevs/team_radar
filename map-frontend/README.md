# Map Frontend workspace

맵 렌더링(캐릭터 배치/시각화) 전용 작업 폴더입니다.

## 담당 범위
- `composed_set_map.html` 렌더링/배치 로직
- 상태별 캐릭터 공간 배치 규칙
- collision overlay 시각화

## 실제 작업 파일
- `/home/teny/.openclaw/workspace/map/composed_set_map.html`

## 실행
```bash
npm run map:serve
```

> `map:serve` 는 `/home/teny/.openclaw/workspace/map` 를 8765 포트로 서빙합니다.
